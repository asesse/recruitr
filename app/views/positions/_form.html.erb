<%= simple_form_for (@position) do |f| %>
  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
  </div>
  <div class="form-actions">
    <%= f.submit "Submit", class: "btn" %>
  </div>
<% end %>
